node_modules/
.env
.env.local
.DS_Store
.idea/
.vscode/
npm-debug.log*
yarn-debug.log*
CLAUDE.local.md

# Runtime data — created on first form submission. Never commit.
# Holds the JSONL submission log + rate-limit state.
data/

# SQLite artifacts (only used by the archived _vps/ Express server, but ignored
# anywhere they appear).
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# Historical Hostinger artifact — third-party PII from the old PHP contact
# form. Kept locally for cold storage; NEVER published.
form_submissions.txt
